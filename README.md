This project deploys a transfer learning model to web with flask
The deployed model is built on top of vgg 16 with custom layers built on classifying the imagenet library
user can directly upload an image to the upload section and the model classifies the image in the 1000 classes of the imagenet
